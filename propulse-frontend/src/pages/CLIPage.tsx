import CLI from '../components/CLI';

// Home Function: This is the homepage, should be located at (/).
const CLIPage: React.FC = () => {  
  return (
    <CLI className='h-screen w-full' />
  )
}

export default CLIPage
